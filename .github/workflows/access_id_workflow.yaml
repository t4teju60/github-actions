name: Print ACCESS_ID

on: [push]

jobs:
  print-access-id:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print ACCESS_ID
      run: echo "The ACCESS_ID is ${{ secrets.ACCESS_ID }}"
